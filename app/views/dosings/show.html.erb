<p id="notice"><%= notice %></p>

<p>
  <strong>Attack:</strong>
  <%= Attack.find(@dosing.attack_id).started_at.to_s.gsub(/:\d\d UTC/, "") %>,
  <%= link_to 'Show', attack_path(Attack.find(@dosing.attack_id)) %>
</p>

<p>
  <strong>Drug:</strong>
  <%= Drug.find(@dosing.drug_id).name %>
</p>

<p>
  <strong>Dosed at:</strong>
  <%= @dosing.dosed_at %> minutes later
</p>

<%= link_to 'Edit', edit_dosing_path(@dosing) %> |
<%= link_to 'Back', dosings_path %>
