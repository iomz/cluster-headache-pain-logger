<%= form_for(@prescription) do |f| %>
  <% if @prescription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prescription.errors.count, "error") %> prohibited this prescription from being saved:</h2>

      <ul>
      <% @prescription.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong>Prescribed at</strong><br>
    <%= f.date_select :prescribed_at %>
  </div>
  <div class="field">
    <strong>Drug</strong><br>
    <%= f.collection_select :drug_id, Drug.all, :id, :name %>
  </div>
  <div class="field">
    <strong>Quantity</strong><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
